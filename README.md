# k0nsult-global-cm — Cameroon (global track, NON-EU)

The **Cameroon** surfaces of the K0NSULT global-expansion track: evidence-first
AI-governance/transparency material (PL + EN).

> **Separate from the EU track by design.** This is non-EU and is kept apart from
> `k0nsult-eu-shield` so the EU sovereignty scope is not diluted; it has its own
> funding/support track (no EU-earmarked resources flow here).

> **Doctrine:** `claim ≤ proof`; only agents are scored, never natural persons.

## Engine stays hidden
`/api/*` calls (if any) are the integration boundary; the k0nsult.cloud engine is
proprietary and **not** in this repository.

## Supply chain
`sbom.json` via [`k0nsult-tools`](../k0nsult-tools).

## License
Apache-2.0. See `LICENSE` and `NOTICE`.
